<h1>New Category</h1>
<%= form_for(@category, url: categories_path(@category), local: true) do |form| %>

<%= render 'form', category: @category, form: form%>

<% end %>
<%= link_to 'Back', categories_path %>
